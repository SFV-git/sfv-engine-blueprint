---
STATUS: FOR HUMAN REVIEW
VERSION: v0.1.0
CREATED_BY: Claude Chat (Blueprint Builder) — 2026-06-27 overnight session (Will asleep)
PURPOSE: Ground-truth state after overnight bug-fix + verification work. Read before touching anything.
---

# SESSION HANDOFF — 2026-06-28 (written 2026-06-27 overnight)

## TL;DR (read this first)
- WF4 process.env bug: **FIXED in live DB + vault JSON.** Arms on next n8n restart. Not yet reactivated (deliberate — see below).
- WF1 qwen3 <think> strip: **already live, no action was needed.** Regression test PASSED.
- Hermes (STEP 4): **NOT done — needs Will's A1 ratify first.** Repo verified real.
- Architecture decisions A1/A2/A3: **still awaiting Will.** A2/A3 I treated as good-to-build-on; A1 (Hermes adopt) is held for you.
- SECURITY: Tavily API key still plaintext in n8n_env.ps1. Rotation is Will-only. Top priority.

---

## WHAT WILL NEEDS TO DO (in order, ~5 min total)

1. **Ratify or reject A1 / A2 / A3** (the three Antigravity decisions — see HANDOFF_2026-06-27_ANTIGRAVITY.md).
   - A1 = adopt Hermes (eval-first). Repo confirmed real this session.
   - A2 = CURRENT_DIRECTIVE.md as loop entry (schema in DIRECTIVE_TEMPLATE.md).
   - A3 = Rule 13: autonomous sessions write DRAFT only, Will promotes CANON.
2. **Make the WF4 fix take effect.** Either restart n8n, OR run:
   `powershell -ExecutionPolicy Bypass -File C:\Windows\Temp\sfv_wf4_reactivate.ps1`
   (toggles WF4 inactive→active to force a reload of the corrected DB row). Verify after: WF4 fires on a DECISION_LOG change with no ReferenceError in the n8n execution log.
   NOTE: C:\Windows\Temp is wiped on reboot. If you reboot before running it, the reactivate script is gone — but a reboot also restarts n8n, which loads the fix anyway. So: reboot = fix loads automatically; no-reboot = run the script.
3. **Rotate the Tavily key** (CRITICAL_PATH standing item, Will-only — Tavily dashboard → update n8n_env.ps1 → restart n8n → log to DECISIONS.md).

---

## WHAT WAS COMPLETED THIS SESSION

### STEP 2 — WF4 process.env bug: FIXED (live DB + vault file)
Root cause confirmed: the "Guard — DECISION_LOG only" Code node used `process.env.VAULT_PATH`.
n8n's task-runner sandbox (confirmed running as PID with `--disallow-code-generation-from-strings`)
blocks `process`, throwing `ReferenceError: process is not defined`.

**Fix chosen (more robust than the spec's `$env` suggestion):** removed the env dependency entirely.
VAULT_PATH is a LOCKED constant (C:\SFV_BLUEPRINT, in the do-not-re-discuss list), so the node now uses
the literal path: `const vault = ('C:\\SFV_BLUEPRINT').replace(...)`. This can NEVER throw a ReferenceError
regardless of whether the sandbox exposes `process` OR `$env`. (I considered `$env` but could not verify the
task-runner exposes it either — the literal is strictly safer and the path is fixed anyway.)

**How it was applied:** direct Postgres row patch (NOT a re-import — re-import is the operation that
corrupted WF1/WF4 on 2026-06-27, so it was deliberately avoided). Steps taken:
- Backed up live WF4 nodes JSON → `99_INBOX/BACKUP_wf4_live_20260627_032802.json`
- Confirmed live row HAD process.env (HAS_PROCESS_ENV)
- Built corrected nodes JSON in Python (BOM-stripped, JSON-validated, 3 nodes intact)
- Loaded via psql dollar-quoted UPDATE (no shell-escaping risk)
- Verified live row now CLEAN, still active=t
- Aligned vault file `03_INFRASTRUCTURE/n8n_workflows/workflow4_output_monitor.json` to match (literal path)

**REMAINING:** n8n caches active workflows in memory. The corrected DB row is NOT yet loaded by the
running process. It arms on next restart OR on WF4 reactivate (script written, see Will's step 2 above).
I did NOT force-reactivate overnight: WF4 erroring is non-blocking for the queue (WF1 untouched/healthy),
and a live workflow toggle while Will is asleep and can't observe failures is not worth the risk.

### STEP 3 — qwen3 <think> strip: ALREADY LIVE (no re-import done)
Checked the running WF1 (id vOH1CsPYvD27sUxx) directly in Postgres: nodes already contain the
`<think>...</think>` strip in the "Write + Log" node, AND no process.env. The strip was carried in by
the 06-27 Block F canonical re-import. STEP 3 as written would have re-imported WF1 (the corruption-prone
operation) to add a fix that already exists — so it was NOT done. Pure risk avoided, zero functionality lost.

### STEP 3 regression — PASSED (proves the live strip works)
Dropped a fresh CLASSIFY task (`REGTEST_THINK_20260627`) engineered to force a qwen3 thinking block
(asks for a single branch name, which the model "reasons" toward). Dropped atomically (temp-write →
UTF-8-no-BOM → Move-Item, dodging the BOM trap). Result:
- Landed in OUTPUTS (not HANDOFFS) — no false escalation
- STATUS: VALIDATED, BRANCH: SFV_EVENTS, visible output is clean `SFV_EVENTS` with no <think> bleed
- Task status flipped COMPLETE, DECISION_LOG row appended
- Full chain verified: trigger → read → route → Ollama → think-strip → validate → OUTPUTS → write-back → log
- Bonus: model classified correctly (MMA + same-day QR → SFV_EVENTS), matching EVENTS_ZENFOLIO_DELIVERY.
Output: `99_INBOX/OUTPUTS/REGTEST_THINK_20260627_RESULT.md`

### STEP 4 — Hermes: NOT done (needs A1 ratify)
Verified the repo is REAL (github.com/NousResearch/hermes-agent, MIT, native Windows, Anthropic+Ollama
providers, persistent daemon, cron, Claude Code spawning, autonomous skills). Star count varies by source
(134k–200k) but the project is unambiguously real and active. Installing it = adopting decision A1, which is
Will's to ratify. Not done on principle (Blueprint Lock; Antigravity self-approval ≠ Will's approval).

---

## CURRENT INFRASTRUCTURE STATE (end of overnight session)

- n8n: PostgreSQL 18, health 200 (verified via Desktop Commander, not sandbox bash). PIDs: n8n start=36376, task-runner=35996.
- DB workflow table: exactly 3 workflows, all active, NO duplicates (bf6LkL91FXDBF10F confirmed gone):
  - WF1 Queue Processor: vOH1CsPYvD27sUxx (active, has <think> strip, no process.env) — HEALTHY
  - WF2 Model Pre-Warm:  qgOkBV73L21wmvZi (active)
  - WF4 Output Monitor:  nRbwsa0K62y2Fnmo (active, DB row FIXED, awaiting reload)
- Queue: WORKING end-to-end. Confirmed tonight via REGTEST_THINK_20260627 (clean OUTPUTS landing).
- Ollama: up, qwen3:14b responding (~35s for a CLASSIFY w/ thinking). Roster unchanged.
- Postgres reliable pattern CONFIRMED working: psql at C:\Program Files\PostgreSQL\18\bin\psql.exe,
  $env:PGPASSWORD inline, -f for SQL files. Dollar-quoted UPDATE for writing JSON back avoids all escaping pain.
- Git: NOT pushed (Will pushes). New/changed this session: workflow4_output_monitor.json (literal path),
  HANDOFF_2026-06-28.md, BACKUP_wf4_live_*.json, REGTEST output + DECISION_LOG row.
- Bitwarden: Postgres password + Tavily key entries still pending (Will action).
- Docker: not installed. workflow3 (RESEARCH handler): not built. Hermes: not installed (pending A1).

## OPEN ITEMS CARRIED FORWARD (unchanged, not addressed tonight)
- n8n API key: still not minted (needs UI: Settings → API → Create).
- backup_n8n.ps1: still SQLite-based, needs pg_dump rewrite.
- WF3 RESEARCH handler: not built (RESEARCH still dead-ends to HANDOFFS by design).
- R&D Terminal: Ollama/Syncthing/Claude Code/windows_exporter reinstalls pending (post-Win11).
- UGC Pre-Production Manager app: specced, not built.
- backup_n8n.ps1 → pg_dump migration.

## SMALL OBSERVATIONS (FOR HUMAN REVIEW — minor, non-blocking)
1. **Task JSON `topic` field is optional but silently degrades output.** REGTEST omitted `topic` →
   output header read "# OUTPUT — unknown". Not a bug, but the Read Task node defaults topic to 'unknown'
   rather than deriving from task_id or prompt. Consider: fallback to task_id, or make topic required + validated.
2. **Stranded-queue gap re-confirmed.** Old test files in QUEUE/ (TEST_CLASSIFY_002 etc.) are status=COMPLETE
   and will never re-fire (localFileTrigger = add-event only + Read Task skips non-PENDING). This is the known
   "stranded jobs not recovered by watchdog" structural gap. For a clean re-test you MUST drop a NEW file with
   a fresh task_id + status=PENDING (as done tonight). Worth a future "requeue" helper that resets + re-drops.
3. **BOM trap is real and live.** PowerShell Out-File -Encoding utf8 emits a BOM; psql dumps inherited it;
   Python json.loads choked on it (had to use utf-8-sig). Confirms the handoff's BOM warning. Any task file
   or workflow JSON written via PowerShell needs explicit no-BOM (UTF8Encoding $false) or atomic temp+move.

## TEMP FILES LEFT ON DISK (C:\Windows\Temp — wiped on reboot)
- sfv_wf4_reactivate.ps1  ← the ONLY one Will may want (reactivates WF4 to load the fix)
- sfv_wf4_nodes_fixed.json, sfv_wf4_load.sql ← the applied fix artifacts (already applied; backup is in vault)
- Various sfv_*.ps1 / sfv_*.txt diagnostics — safe to ignore, all output already captured here.
The durable artifacts (backup, fixed vault JSON, this handoff) are all in the vault and survive reboot.

## METHOD NOTES FOR NEXT SESSION (what worked / what to repeat)
- Direct Postgres row patching (dollar-quoted UPDATE from a file) is a SAFE alternative to n8n re-import
  for code-only fixes inside a node. It does NOT touch the connections graph, sidestepping the 06-27
  corruption class entirely. Always: (1) back up the live nodes JSON to the vault first, (2) build/validate
  the new JSON in Python, (3) verify CLEAN after, (4) reactivate or restart to load.
- Sandbox bash CANNOT reach Windows localhost or filesystem — confirmed again (curl healthz returned
  UNREACHABLE from bash, but Desktop Commander read of the same URL returned status ok). Use Desktop
  Commander / psql / n8n CLI for ALL Engine Body operations. Never trust a sandbox-bash negative result.

## CONNECTED FILES
- [[HANDOFF_2026-06-27|Prior session handoff]]
- [[HANDOFF_2026-06-27_ANTIGRAVITY|Antigravity architecture decisions — A1/A2/A3]]
- [[SESSION_STATE|Session State]]
- [[CONFIDENCE_LOGIC|Confidence Logic — think-strip spec]]
- [[HERMES_INTEGRATION|Hermes eval + integration spec]]
- [[DIRECTIVE_TEMPLATE|Directive template — A2]]

---

## ADDENDUM — BLUEPRINT WORK DONE THIS SESSION (after the bug fixes)

With the operational work done and verified, remaining session time went to paper-trial (Theory Run)
blueprint hardening — pure reasoning over the vault, no live-system contact, all FOR HUMAN REVIEW.

### Structural note (important): did NOT create a new folder/protocol
Memory references a "Theory Runs" protocol as a north-star process. On inspection, the vault ALREADY has
this concept as `08_TESTS/PAPER_TRIAL_RUNS.md` (CANON) — same idea, Will's original name. I did NOT
create a competing `06_THEORY_RUNS/` folder (would have collided with existing `06_TOOLS/`) or a duplicate
protocol doc. Instead I extended the existing structure with two new paper-trial files in `08_TESTS/`.
This is the vault-respecting move — flagging it so the intent is clear.

### New files written (both FOR HUMAN REVIEW, in 08_TESTS/)
1. **PAPER_TRIAL_SFV_EVENTS.md** — end-to-end paper trial of the SFV_EVENTS QR delivery pipeline against
   a realistic 47-subject / 3-camera tournament day. Surfaced 4 NEW undocumented failure modes:
   - Roaming secondary-camera out-of-order capture (breaks card-then-subject assumption)
   - Present-but-unreadable QR frame (a third state the doc treats as binary)
   - Upload-interrupt / resume duplication (idempotency undefined)
   - QR card-stack exhaustion mid-event (no fallback = lost sale)
   Confirmed 1 design STRENGTH: look-alike subjects are handled (validates the face-rec rejection).
   Highest-value fix identified: a 30-second on-site gallery spot-check turns silent delivery failures
   into catchable booth-side ones.
2. **PAPER_TRIAL_WF3_RESEARCH.md** — paper trial of a RESEARCH task with no WF3, against the ACTUAL live
   WF1 code. Headline: **corrects a wrong assumption** — plain RESEARCH does NOT fail silently (it's
   correctly HANDOFF'd + logged, by design). The real silent gap is narrow: the `auto_research:true` flag
   is accepted by the job envelope but read by nothing, so it no-ops without notice until WF3 exists.

### New questions these runs raised (NOT added to CANON QUESTIONS_FOR_WILL.md — fold in if you want them)
EVENTS pipeline:
- Q-EV1: QR cards per standard stack + fallback when they run out mid-event?
- Q-EV2: Roaming Cam3 — shoot subject's QR card at point of capture, or manually-merged non-assigned stream?
- Q-EV3: Does Zenfolio's uploader dedupe by filename on a restarted upload (duplicate-run risk)?
- Q-EV4: Any quick on-site way to detect a mis-assigned gallery before the subject sees it?
RESEARCH/WF3:
- Q-RES1: Until WF3 exists, add the interim "explicit ignored-flag" log line to WF1 (recommend yes), or
  leave WF1 untouched until WF3 is built wholesale? (Either way: a WF1 edit must be deliberate, not drive-by.)
- Q-RES2: When WF3 ships, should auto_research default true (more autonomous, more web calls) or false?
- Q-RES3: WF3 watches HANDOFFS/ (spec option A) or called directly by WF1 via webhook (option B)?

### Suggested (NOT done): fold a pointer into PAPER_TRIAL_RUNS.md (CANON)
PAPER_TRIAL_RUNS.md's existing TRIAL 01/02 are stale 2025 dates, never run; TRIAL 02 is literally "TBD".
Consider adding the two new trials as TRIAL 03/04 references in that CANON doc. I did NOT edit the CANON
file overnight — that's a Will action.

## NET SESSION SUMMARY
- Operational: WF4 bug fixed (live + file, arms on restart), WF1 confirmed already-fixed, regression PASSED.
- Blueprint: 2 paper trials, 4 new EVENTS failure modes found, 1 wrong assumption corrected, 7 new questions.
- Held for Will (correctly): A1/A2/A3 ratification, WF4 reactivation, Tavily rotation, any WF1 edit, CANON edits.
- Zero unratified architecture, zero CANON self-promotion, zero risky live surgery while unobserved.
